<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ohgiraffers.team3backendadmin.admin.query.mapper.DepartmentMapper">

    <resultMap id="departmentResultMap"
               type="com.ohgiraffers.team3backendadmin.admin.query.dto.response.DepartmentResponse">
        <id property="departmentId" column="department_id"/>
        <result property="parentDepartmentId" column="parent_department_id"/>
        <result property="departmentName" column="department_name"/>
        <result property="teamName" column="team_name"/>
        <result property="depth" column="depth"/>
        <result property="createdAt" column="created_at"/>
        <result property="createdBy" column="created_by"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="updatedBy" column="updated_by"/>
    </resultMap>

    <select id="findById" resultMap="departmentResultMap">
        SELECT department_id,
               parent_department_id,
               department_name,
               team_name,
               depth,
               created_at,
               created_by,
               updated_at,
               updated_by
          FROM department
         WHERE department_id = #{departmentId}
    </select>

    <select id="findAll" resultMap="departmentResultMap">
        SELECT department_id,
               parent_department_id,
               department_name,
               team_name,
               depth,
               created_at,
               created_by,
               updated_at,
               updated_by
          FROM department
         ORDER BY department_id
    </select>

</mapper>
